import { expect } from 'chai'
// import { unitReducer } from './unit-reducer'

describe('setupReducer', () => {
  it('should create initial state', () => {
    // const state = unitReducer([], {type: ''})
    // expect(state).to.eql({})
  })
})
